:80 {
    # Route API endpoints and backend health checks to the FastAPI container
    reverse_proxy /health backend:8000
    reverse_proxy /api/* backend:8000

    # Route WebSocket connections for real-time data streaming
    reverse_proxy /ws/* backend:8000

    # Route all other web and asset traffic to the Vite frontend container
    reverse_proxy frontend:5173
}